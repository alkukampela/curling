version: '2'

services:
  gamemanager:
    build: 
      context: '.'
      dockerfile: Dockerfile-dev
    ports:
     - '9600:5000'
    volumes:
     - '.:/code'
    environment:
     - PYTHONUNBUFFERED=0
