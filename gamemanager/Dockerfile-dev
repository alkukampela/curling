FROM python:alpine

RUN mkdir /code

ADD . /code

WORKDIR /code

RUN apk add --no-cache --virtual .build-deps \
    g++ \
    && pip3 install -r requirements.txt \
    && apk del .build-deps

CMD ["python", "app.py"]
